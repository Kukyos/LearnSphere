{
  "name": "learnsphere-server",
  "version": "1.0.0",
  "description": "LearnSphere API Server — Auth, Courses, Enrollments, Progress, Reviews",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "set NODE_ENV=development && node server.js",
    "db:init": "psql -U postgres -d learnsphere -f schema.sql",
    "seed": "node seed.js"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}
